#ifndef corr_H
#define corr_H
#include <vector>
#include <string>
using namespace std;
namespace correlate{
	void compute(vector<vector<double>>, vector<double>, vector<int>, vector<string>, vector<double>, string );
}
#endif
